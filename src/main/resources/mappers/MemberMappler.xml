<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csq.mapper.MemberMapper">

    <select id="getAllMember" parameterType="java.lang.Integer" resultType="com.csq.entity.Member">
        select * from member where 1=1
        <if test="limit != 0">
            limit #{startPage}, #{limit}
        </if>
    </select>
    <select id="getCount" resultType="java.lang.Integer">
        select count(*) from meterial
    </select>
    <select id="getMemberById" parameterType="java.lang.Integer" resultType="com.csq.entity.Member">
        select * from member where id = #{id}
    </select>
    <update id="editMember" parameterType="com.csq.entity.Member">
        update member
        <trim prefix="set" suffixOverrides=",">
            <if test="name!=null">name=#{name},</if>
            <if test="sex!=null">sex=#{sex},</if>
            <if test="phoneNumber!=null">phone_number=#{phoneNumber},</if>
            <if test="password!=null">password=#{password},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteMemberById" parameterType="java.lang.Integer">
   	 	delete from member where id = #{id}
   	 </delete>

    <insert id="addMember" parameterType="com.csq.entity.Member">
        insert into member
        (name,
        sex,
        password,
        phone_number
        )
        values
            (
            #{name},
            #{sex},
            #{password},
            #{phoneNumber}
            )
    </insert>

</mapper>